# CI-only requirements — excludes private Emergent packages (emergentintegrations, litellm)
# that are not on public PyPI. AI endpoints use lazy imports, so the API can start without them.
fastapi==0.110.1
uvicorn==0.25.0
motor==3.3.1
pymongo==4.6.3
pydantic==2.13.4
pydantic_core==2.46.4
email-validator==2.3.0
PyJWT==2.13.0
bcrypt==4.1.3
resend==2.36.0
python-multipart==0.0.32
python-dotenv==1.2.2
requests==2.34.2
pytest==9.1.1
pytest-xdist==3.8.0
httpx==0.28.1
